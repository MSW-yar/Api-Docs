const fs = require('fs');
const path = require('path');

// // creating new folder.
// fs.mkdir(path.join(__dirname, '/text'), {}, err => {
//     if (err) throw err;
//     console.log('folder created...');
    
// })

// writing in a new folder.

// fs.writeFile(path.join(__dirname, '/text', 'hello.txt'), 'hello ahah', err =>{
//     if (err) throw err
//     console.log('File written to ...');
    
// })

